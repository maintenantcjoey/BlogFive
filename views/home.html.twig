{% extends "base.html.twig" %}

{% block body %}
    <div class="container">
    
        <h1>Jessica Legrand, dévelopeuse</h1>

        <div class="m-5">
            <h5>Enthousiaste et déterminée, je souhaite mettre mes connaissances et mon
            énergie à votre service pour relever de nouveaux défis, afin de m'enrichir, de
            développer mes compétences ainsi que ma créativité.
            </h5>
        </div>

        <div class="row justify-content-between">
            {% for post in posts%}
            <div class="col mt-5">
                <div class="card mb-3" style="width: 17rem;">
                    <div class="card-body">
                        <h5 class="card-title">{{post.title}}</h5>
                        <p class="card-text">{{post.chapo}}</p>
                        <a href="/post/{{post.id}}" class="btn btn-primary">Lire l'article</a>
                    </div>
                </div>
                </div>
            {% endfor%}
        </div>

        <div class="card m-5">
        <img src="../public/assets/image/cv.jpg" class="card-img-bottom" alt="cv">
        </div>
            
        <div class="d-flex justify-content-center">
            <button class="btn btn-dark" type="submit">Télécharge le CV</button>
        </div>

        <form action="/contact" method="post">
            <div class="m-5">
                <div class="mb-3">
                    <label for="exampleFormControlInput1" class="form-label">Votre Email</label>
                    <input type="email" class="form-control" name="email" id="exampleFormControlInput1" placeholder="name@example.com">
                </div>
                <div class="mb-3">
                    <label for="exampleFormControlTextarea1" class="form-label">Votre Messsage ici</label>
                    <textarea class="form-control" name="message" id="exampleFormControlTextarea1" rows="3"></textarea>
                </div>
                <div class="d-flex justify-content-center">
                    <button class="btn btn-dark" type="submit">Envoyer</button>
                </div>
            </div>
        </form>
    </div>
{% endblock %}